// export class Supplier {
//     _id: string;
//     name: string;
//     city: string;
//     reference: string;
//     status: string;
// }

// Seems to work the same way with an interface instead of a class
export interface Supplier {
    _id: string;
    name: string;
    city: string;
    reference: string;
    status: string;
}